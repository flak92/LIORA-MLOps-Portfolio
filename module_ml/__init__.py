"""ML research layer: the feature set an asset's model sees, triple-barrier labels, purged walk-forward, XGBoost, the strategy simulation, and the reports — ml_status.json and each asset's README."""
