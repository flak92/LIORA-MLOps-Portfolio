# Crawler's mission

You are reviewing one file of this repository against its written rules. The rules follow this
mission, each under its path; the file follows them, under its own path. Everything you need is in
this message — read nothing else.

Report, in this order:
1. every departure from a rule: `<rule path> § <section or grammar row>`, the line of the file, the
   current form, the form the rule derives — one per line;
2. what the file leaves unexplained: a name, a placement or a sentence a reader cannot decode from
   the file and the rules alone;
3. a pattern seen twice or more that no rule governs: the pattern and the form it would forbid;
4. one closing line: `conformant` or `departures: <n>`.

Cite a rule, never restate it. Propose, never decide. Do not rewrite the file.
